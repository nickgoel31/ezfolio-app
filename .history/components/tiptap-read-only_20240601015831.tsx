import React from 'react'

const TiptapReadOnly = ({description, onChange}:{description:string, onChange: (richText:string) => void}) => {
  return (
    <div>TiptapReadOnly</div>
  )
}

export default TiptapReadOnly